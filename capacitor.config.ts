import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joyering.app',
  appName: 'Joyering',
  webDir: 'build',
  // @ts-ignore
  appScheme: 'joyering'
};

export default config;